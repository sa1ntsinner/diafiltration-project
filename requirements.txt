# ── required (the packages allowed by the task sheet) ───────────────────────
numpy>=1.23
casadi>=3.6
matplotlib>=3.7

# ── optional: interactive dashboard only (never imported by the report) ─────
streamlit>=1.32

# ── optional: development ───────────────────────────────────────────────────
pytest>=7.0
scipy>=1.10        # convenience only; the package has a pure-NumPy fallback
