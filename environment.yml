name: diafiltration_mpc_SS25
channels:
  - conda-forge
dependencies:
  - python=3.9
  - numpy
  - matplotlib
  - casadi=3.6.*        # есть сборки и под Linux
  - pytest
  # Dev-утилиты (pip-route, не ломают solver)
  - pip